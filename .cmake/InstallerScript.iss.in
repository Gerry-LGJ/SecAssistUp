; Script generated by the Inno Setup Script Wizard.
; SEE THE DOCUMENTATION FOR DETAILS ON CREATING INNO SETUP SCRIPT FILES!

#define MyAppName           "${PROJECT_NAME}"
#define MyAppVersion        "${GIT_SEMVER}"
#define MyAppFileVersion    "${GIT_SEMVER}.${GIT_COMMIT_COUNT}"
#define MyAppPublisher      "TimeChicken"
#define MyAppCopyright      "Copyright (C) 2023 TimeChicken. All rights reserved."
#define MyAppURL            "https://github.com/Gerry-LGJ/SecAssistUp"
#define MyAppExeName        "${PROJECT_NAME}.exe"
#define MyAppFileDir        "dist"
#define MyAppSourceDir      ".\.."
#define MySignTool          "mysigntool"
#define MyAppDataDir        "{localappdata}\TimeChicken\${PROJECT_NAME}"
#define MyAppId             "693E34DD-2065-488E-B2C1-C91CBE20CAC8"

[Setup]
; NOTE: The value of AppId uniquely identifies this application. Do not use the same AppId value in installers for other applications.
; (To generate a new GUID, click Tools | Generate GUID inside the IDE.)
AppId={{{#MyAppId}}
AppName={#MyAppName}
AppVersion={#MyAppVersion}
;AppVerName={#MyAppName} {#MyAppVersion}
AppPublisher={#MyAppPublisher}
AppPublisherURL={#MyAppURL}
AppSupportURL={#MyAppURL}
AppUpdatesURL={#MyAppURL}
AppCopyright={#MyAppCopyright}
VersionInfoVersion={#MyAppFileVersion}
DefaultDirName={autopf}\{#MyAppName}
UninstallDisplayIcon={app}\{#MyAppExeName}
DisableProgramGroupPage=yes
LicenseFile={#MyAppSourceDir}\License
; Uncomment the following line to run in non administrative install mode (install for current user only).
;PrivilegesRequired=lowest
OutputDir=.\
OutputBaseFilename={#MyAppName}-{#MyAppVersion}_setup
Compression=lzma
SetupIconFile={#MyAppSourceDir}\res\image\favicon_nbg.ico
SolidCompression=yes
WizardStyle=modern
; build x64
; "ArchitecturesAllowed=x64compatible" specifies that Setup cannot run
; on anything but x64 and Windows 11 on Arm.
ArchitecturesAllowed=x64
; "ArchitecturesInstallIn64BitMode=x64compatible" requests that the
; install be done in "64-bit mode" on x64 or Windows 11 on Arm,
; meaning it should use the native 64-bit Program Files directory and
; the 64-bit view of the registry.
ArchitecturesInstallIn64BitMode=x64
; sign tool
; SignTool={#MySignTool}
; SignedUninstaller=yes
DisableDirPage=auto
ShowLanguageDialog=auto

[Languages]
Name: "english"; MessagesFile: "compiler:Default.isl"
Name: "chinesesimplified"; MessagesFile: "compiler:Languages\ChineseSimplified.isl"

[CustomMessages]
chinesesimplified.Msg_InquiryKeepData=是否保留个人数据？%n这包括您创建的所有工程和设置信息%n路径：%1 %n%n选择【是】将保留您的个人数据；%n选择【否】将清除所有个人数据。
english.Msg_InquiryKeepData=Keep personal data? %nThis includes all your projects and settings%nPath: %1 %n%nSelect [Yes] to keep your personal data;%nSelect [No] to delete all personal data.


[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: checkablealone

[Files]
Source: "{#MyAppSourceDir}\{#MyAppFileDir}\{#MyAppExeName}"; DestDir: "{app}"; Flags: ignoreversion signonce
Source: "{#MyAppSourceDir}\{#MyAppFileDir}\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
; NOTE: Don't use "Flags: ignoreversion" on any shared system files

[Icons]
Name: "{autoprograms}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon

[Run]
Filename: "{app}\{#MyAppExeName}"; Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; Flags: nowait postinstall skipifsilent

[Registry]
; Delete the startup items for the current user
Root: HKCU; Subkey: "Software\Microsoft\Windows\CurrentVersion\Run"; ValueType: none; ValueName: "{#MyAppName}"; Flags: deletevalue uninsdeletevalue;

[Code]
function GetUILanguage: Integer;
external 'GetUserDefaultUILanguage@kernel32.dll stdcall';

function CmdLineParamExists(const ParamName: String): Boolean;
var
  i: Integer;
begin
  Result := False;
  for i := 1 to ParamCount do
  begin
    if CompareText(ParamStr(i), ParamName) = 0 then
    begin
      Result := True
      Exit;
    end;
  end;
end;

function CmdLineGetParamValue(const ParamName: String; const DefaultValue: String): String;
var
  i: Integer;
begin
  Result := DefaultValue
  for i := 1 to ParamCount do
  begin
    if CompareText(Copy(ParamStr(i), 1, Length(ParamName) + 1), ParamName + '=') = 0 then
    begin
      Result := Copy(ParamStr(i), Length(ParamName) + 2, Length(ParamStr(i)));
      Log(Format('Found Param %s, value is %s', [ParamName, Result]));
      Exit;
    end;
  end;
end;

function GetUninstallString(): String;
var
  sUninstPath        : String;
  sUninstallString   : String;
begin
  sUninstPath        := 'Software\Microsoft\Windows\CurrentVersion\Uninstall\{{#MyAppId}}_is1'
  sUninstallString   := '';
  if not RegQueryStringValue(HKLM, sUninstPath, 'UninstallString', sUninstallString) then
    RegQueryStringValue(HKCU, sUninstPath, 'UninstallString', sUninstallString);
  Result := sUninstallString
end;

function RunUninstall(const Param: String): Boolean;
var
  sUninstallString: String;
  iResultCode     : Integer;
begin
  Result := False;
  sUninstallString := GetUninstallString();
  if sUninstallString <> '' then
  begin
    sUnInstallString := RemoveQuotes(sUnInstallString);
    if Exec(sUninstallString, Param, '', SW_SHOWNORMAL, ewWaitUntilTerminated, iResultCode) then
    begin
      Log('The silent uninstallation command for the old version has been executed, and the uninstallation process has been completed.');
      Result := True;
    end;
  end;
end;

function InitializeSetup(): Boolean;
var
  sUninstallString: String;
begin
  Result := True

  if CmdLineParamExists('/AUTOUNINSTALL') then
  begin
    RunUninstall('/SILENT /SUPPRESSMSGBOXES /NORESTART');
  end;

end;

procedure CurStepChanged(CurStep: TSetupStep);
var
  ResultCode: Integer;
begin
  if (CurStep = ssDone) and CmdLineParamExists('/LAUNCHAPP') and CmdLineParamExists('/SILENT') then
  begin
    if Exec(ExpandConstant('{app}\{#MyAppExeName}'), '', '', SW_SHOWNORMAL, ewNoWait, ResultCode) then
    begin
      Log('Launch Program Succeed.');
    end else
    begin
      Log(Format('Launch Program Failure. code:%d', [ResultCode]));
    end;
  end;
end;

procedure CurUninstallStepChanged(CurUninstallStep: TUninstallStep);
var
  DataPath: String;
  MsgText:  String;
begin
  if CurUninstallStep = usUninstall then
  begin
    DataPath := ExpandConstant('{#MyAppDataDir}');

    MsgText := ExpandConstant('{cm:Msg_InquiryKeepData,' + DataPath + '}');
    Log('MsgText.Msg_InquiryKeepData:' + #13#10 + MsgText);
    if not CmdLineParamExists('/SILENT') and not CmdLineParamExists('/VERYSILENT') then
    begin
      if MsgBox(MsgText, mbConfirmation, MB_YESNO) = IDNO then
      begin
        DelTree(DataPath, True, True, True);
      end;
    end;

  end;
end;

